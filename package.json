{
  "name": "jalexw-resume",
  "description": "My personal résumé. Created with the LaTeX document preparation system.",
  "version": "1.0.0",
  "scripts": {
    "watch": "ts-node watch.ts",
    "generate": "pdflatex -jobname=resume resume.tex"
  },
  "devDependencies": {
    "@types/node": "^20.5.1",
    "chokidar": "^3.5.3",
    "ts-node": "^10.9.1"
  }
}
